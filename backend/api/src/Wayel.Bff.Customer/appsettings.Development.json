{
  // Local-developer overrides for Wayel.Bff.Customer.
  //
  // !!! DEV-ONLY GOOGLE OIDC CREDENTIALS INLINED BELOW !!!
  // Populated from this session's user-secrets. Rotate + relocate to
  // `dotnet user-secrets` before sharing this branch.
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information",
        "Yarp": "Information"
      }
    }
  },
  "Bff": {
    "RequireHttpsCookie": false,
    "ApiBaseUri": "http://localhost:5099",
    "SpaBaseUri": "http://localhost:4202"
  },
  "GoogleOidc": {
    // Google OAuth web client for the client-portal BFF (wayel-494011 / port 5198).
    // Real values live outside source control. Restore locally via:
    //   cd backend/api/src/Wayel.Bff.Customer
    //   dotnet user-secrets set "GoogleOidc:ClientId"     "<client-id>"
    //   dotnet user-secrets set "GoogleOidc:ClientSecret" "<client-secret>"
    // See `.secrets.local/google-oidc.md` for the current dev values.
    "ClientId": "",
    "ClientSecret": ""
  }
}
