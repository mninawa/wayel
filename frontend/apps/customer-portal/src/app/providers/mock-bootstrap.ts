import { Provider } from '@angular/core';

/** Mock bootstrap disabled — use sign-in / Google SSO to start the journey. */
export function provideBorderboxMockBootstrap(): Provider[] {
  return [];
}
